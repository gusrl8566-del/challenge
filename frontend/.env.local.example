# Local development (recommended)
# NEXT_PUBLIC_API_URL is unset so the frontend uses /api rewrite.

# If you need direct API calls in local browser, uncomment below.
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
