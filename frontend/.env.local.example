# Local development
# Next rewrite destination used by /api proxy in dev/build.
NEXT_INTERNAL_API_URL=http://localhost:3001/api

# Keep this unset in local by default so browser calls /api.
# NEXT_PUBLIC_API_URL=
